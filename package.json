{
  "name": "samples",
  "version": "1.0.0",
  "description": "sample git repo",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ssr",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
